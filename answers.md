# Формат вопросов и ответов

### Вопрос

```
код, указанный в вопросе
```

* Ответ (Если в скобках рядом с ответом стоит "?", значит этот ответ не факт, что верный)

# Вопросы

### 1 Какой зависимостью описывается функция вычислительной сложности алгоритма пирамидальной сортировки (Heapsort) в среднем случае

* f(n)=O(nlog(n))

### 2 Определите теоретическую вычислительную сложность (функцию роста времени) алгоритма: 
```
for (int i=n/2; i<n; i++>)
    for (int j=1; j<n; j=j*2){
        /* Последовательность шагов программы с временной сложностью О(1)  */
    }
```

* линейно-логарифмическая

### 3 Для оценки порядка роста функций‚ описывающих вычислительную сложность алгоритмов, используются асимптотические обозначения (СИМВОЛИКИ) или нотации. Что обозначает запись f(n)=0(g(n)):

* Множество всех функций, порядок роста которых при достаточно больших п не больше(меньше или равен) некоторой константы C, умноженной на значение функции g(n)

### 4 B теории вычислимости важную роль играет функция Аккермана A(m, n), определённая следующим образом: *что то дано*. Вычислите значение A(2, 2). (Введите только число)

* 7

### 5 Какой зависимостью описывается функция вычислительной сложности алгоритма поиска по бинарному дереву поиска (binary search tree, BST) в лучшем случае?

* f(n)=Ө(log(n))

### 6 Какие существуют случаи в анализе алгоритма:

* Наилучший, средний и наихудший

### 7 Какая операция считается недопустимой дляЯ линейного односвязного списка:

* Вставка нового элемента перед заданным элементом

### 8 К графу на рисунке 1 применен алгоритм обхода в ширину. Кaкoe из остовных деревьев соответствует этому алгоритму обхода с вершины 3 **дан рисунок**?

* 4

### 9 Какие отношения между элементами поддерживает структура данных бинарное дерево?

* Иерархические

### 10 Алгоритм Прима - это:

* Алгоритм поиска минимального остовного дерева во взвешенном неориентированном связном графе

### 11 В результате применения алгоритма RLE был получен сжатый текст 9A-4BCAB7C. Какой коэффициент сжатия этого текста обеспечил алгоритм RLE

* 2 (?)

### 12 Для структуры данных очередь справедливо

* даление элемента возможно с одной из сторон списка

### 13 то измеряется для оценки временной (ВЫЧИСЛИТеЛЬНОЙ) сложности алгоритма:

* Зависимость количества выполняемых основных операций от объёма входа

### 14  какой последовательности располагаются вершины дерева при прямом обходе (preorder):

* , 2, 4, 8, 9, 5, 10, 11, 3, 6, 12, 13, 7, 14, 15

### 15 Структура данных массив отображается на физическом уровне в структуру хранения:

* вектор

### 16 Какой зависимостью описывается функция вычислительной сложности алгоритма поиска в тексте по образцу методом Кнута-Морриса-Пратта в лучшем случае?

* f(n, m)=Ө(n+m)

### 17 Понятие "глубина рекурсии" для рекурсивной функции определяет:

* Наибольшее одновременное количество рекурсивных обращений функции

### 18 Есть ли ошибка в коде фуНКЦИИ f, которая осуществляет получение ссылки на ПОСЛедНИЙ узел линейного односвязного списка со структурой узла?

```
struct Tnode{ Tdata data; Tnode* next;};
Tnode *f(Tnode *L){
    Tnode *q=L;
    while(q) { q=q->next;}
    return q;
}
```

* Ошибка в условии while (q) - должно быть while (g->next)

### 19 Исходный текст "abedabedaaacc” был сжат, в результате был получен код abed(4,4)(8,1)(1,1)(1,1) (5,1). Какой принцип сжатия текстовой информации при этом использовался?

* уменьшение объема текста за счет замены повторно встретившейся цепочки символов ссылкой на ранее встреченную цепочку

### 20 Какой зависимостью описывается функция вычислительной сложности алгоритма вставки ключа в бинарное дерево поиска в наилучшем случае?

* f(n)=O(log(n))

### 21 Как называется алгоритм, который выполнит сортировку исходного массива (3,1,5,2,4) следующей последовательностью проходов

```
(1, 2, 5, 2, 4), (1, 2, 5, 2, 4), (1, 3, 2, 5, 4), (1, 2, 5, 2, 4)
(1, 2, 5, 2, 4), (1, 2, 5, 2, 4), (1, 3, 2, 5, 4), (1, 2, 5, 2, 4)
```

* Простого обмена

### 22 Какая функция реализует линейную рекурсию:

* Содержит один вызов самой себя 

### 23 Какой зависимостью описывается фуНКЦИЯ вычислительной сложности алгоритма сортировки методом Шелла в наилучшем случае?

* f(n)=(nlog(n))

### 24 Как называется алгоритм поиска минимального остовного дерева во взвешенном неориентированном связном графе?

* Алгоритм Крускала

### 25 Как называется алгоритм, который выполнит сортировку исходного массива (3,1,5,2,4) следующей последовательностью проходов

```
(1, 3, 5, 2, 4), (1, 3, 5, 2, 4), (1, 2, 3, 5, 4), (1, 2, 3, 4, 5)
```

* Простого выбора

### 26 Дисциплина обслуживания (порядок выполнения операций) очереди:

* FIFO

### 27 Как называется алгоритм, который выполнит сортировку исходного массива (3,1,5,2,4) следующей последовательностью проходов

```
(3, 1, 4, 2, 5), (3, 1, 2, 4, 5), (2, 1, 3, 4, 5), (1, 2, 3, 4, 5)
```

* Простой вставки

### 28 Что определяет структура данных

* Множество данных и отношений между ними

### 29 Из числа приведённых сортировок выберите наименее эффективную на больших массивах

* пузырьковая

### 30  основе алгоритма Бойера-Мура-Хорспула используется

* Эвристика стоп-символов ("плохих" символов)

### 31 Что предусматривает метод динамического программирования?

* Запоминание результатов решения Tex подзадач, которые могут повторно использоваться

### 32 Какой зависимостью описывается фуНКЦИЯ вычислительной сложности алгоритма сортировки шейкерным методом (C условием Айверсона) в наихудшем случае

* f(n)=O(n^2)

### 33 При каком значении баланс-фактора b (T.e. разницы высот правого и левого поддеревьев) необходимо выполнить перестройку АВЛ-дерева?

* |b|=2

### 34 Как называется алгоритм, который напрямую или через другие вспомогательные алгоритмы вызывает сам себя:

* Рекурсивная

### 35 Какой алгоритм из перечисленных не основан на жадном подходе?

* Алгоритм нахождения кратчайшего пути Беллмана-Форда (?)

### 36 Идея алгоритма интерполяционного поиска основана на

* Выборе новой области поиска по расстоянию между ключом и текущим значением элемента
* Учёте знака разности между ключом и текущим значением элемента

### 37 Что помещается в стек рекурсивных вызовов при вызове рекурсивной функции?

* Адрес точки возврата, значения всех переменных функции текущего вызова

### 38 Выберите код, являющийся результатом сжатия MO методу скользящего окна текста "abacabacabadaca”. Примечание: кавычки ограничивают текст и не кодируются

* (0,0,а)(0,0,5)(2,1,с)(4,7,а)(2,1,с)(2,1,пусто) 

### 39 Какой зависимостью описывается функция вычислительной сложности алгоритма поиска хешированием в лучшем случае?

* f(n)=Ө(1)

### 40 Отличительная особенность алгоритма Бойера-Мура

* Сравнение символов производится начиная с конца образца

### 41 Пример какой структуры данных изображён на рисунке **дан хуй пойми какой рисунок**

* дек (? (синк пишет, что ответ верный, но проверить без рисунка невозможно))

### 42  каких случаях алгоритм КНУТЗ-МОррИСЭ-ПраТТЭ дает ПОДЛИННЫЙ выигрыш по сравнению с другими алгоритмами поиска в тексте по образцу

* Когда неудачному сравнению образца с текстом предшествовало некоторое число совпадений

### 43 Какой зависимостью описывается функция вычислительной сложности алгоритма бинарного (двоичного) поиска в худшем случае?

* f(n)=Ө(log(n))

### 44 Линейные списки находят применение в технологии

* распределённых систем блокчейн 

### 45 Определите теоретическую вычислительную сложность (функцию роста времени) алгоритма

```
for (int i=0; i<n; i++)
    for (int j=0; j<n; j++)
        cout<<"hello";
```

* квадратичная

### 46 Укажите правильную аналогию между асимптотическим сравнением двух функций f и g для обозначения f(n)=Ω(g(n)) и сравнением двух действительных чисел а и b

* a>=b

### 47 Укажите правильную аналогию между асимптотическим сравнением двух функций Х и g для обозначения f(n)=0(g(n)) и сравнением двух действительных чисел а и b

* a<=b

### 48 Основное требование‚ предъявляемое K массиву для возможности выполнения двоичного поиска

* Упорядоченность массива 

### 49 В основе алгоритма Кнута-Морриса-Пратта используется

* Префикс-функция

### 50 Какой зависимостью описывается фуНКЦИЯ вычислительной сложности алгоритма поиска по бинарному дереву в худшем случае (несбалансированное бинарное дерево)?

* f(n)=Ө(n)

### 51 Какие нотации можно использовать для асимптотической оценки сложности алгоритма в худшем случае?

* нотация Ө (тета)
* нотация O (o-большое)

### 52 Дисциплина обслуживания (порядок выполнения операций) стека

* LIFO

### 53 Величина сдвига при поиске образца в тексте определяется наибольшим значением из трёх, вычисленных, соответственно, по трём правилам (эвристикам) в алгоритме

* Бойера-Мура с турбосдвигом 

### 54 Какой зависимостью описывается функция вычислительной сложности алгоритма поиска
хешированием в худшем случае?

* f(n)=Ө(n)

### 55 Отличительная особенность алгоритма Бойера-Мура-Хорспула

* После каждого неудачного сравнения производится сдвиг образца вправо в соответствии с эвристикой стоп-символов ("плохих" символов)

### 56 Какие основные операции следует учитывать при оценке временной (ВЫЧИСЛИТЕЛЬНОЙ) сложности алгоритмов поиска

* Операции сравнения и перемещения данных (?)

### 57 Какой зависимостью описывается фуНКЦИЯ вычислительной сложности алгоритма поиска в тексте по образцу методом Бойера-Мура в лучшем случае?

* f(n, m)=Ө(log(n/m))

### 58 Структура данных, работа с элементами которой организована по принципу FIFO (первый пришел - первый ушел), - это

* очередь

### 59 Сложность рекурсивного алгоритма вычисления №-ого числа Фибоначчи можно снизить с О(2^п) до O(n), т.е. оптимизировать процесс вычисления, если сохранять промежуточные значения решения подзадач. Какой метод построения алгоритма (алгоритмическая стратегия) здесь задействован?

* Динамическое программирование

### 60 Какие из представленных связных неориентированных графов являются Эйлеровыми (содержат цикл Эйлера)?

* 1 (?)

### 61 Что будет результатом применения алгоритма RLE - "Групповое кодирование" к сжатию текста AAAAAAAAAAAAAAAAAABCABCACCCCCC?

* А18АВСАВС6С  (?)

### 62 Массив длиной п сортируется пузырьковым алгоритмом MO неубыванию. За сколько проходов по массиву наименьшее значение массива окажется "вверху"?

* n-1 

### 63 В каком типе деревьев лист не должен содержать значение?

* В-дерево

### 64 Какой зависимостью описывается фуНКЦИЯ вычислительной сложности алгоритма поиска ключа в бинарном дереве поиска в наихудшем случае?

* f(n)=O(n)

### 65 Имеется следующее оптимальное кодовое дерево (ОКД): **дано дерево**. Выберите исходную строку, которая была закодирована B битовый код 111 0 0 10 10 0 110 0 10 0 по алгоритму Хаффмана с использованием приведенного OKД

* BDDCCDADCD

### 66 Какой зависимостью описывается функция вычислительной сложности алгоритма сортировки слиянием (Mergesort) в среднем и наихудшем случаях

* f(n)=Ө(nlog(n))

### 67 Дано описание функции
```
int f(int x) {return (x==0)? 1:x*f(x-1);}
```
Данная функция вычисляет

* факториал числа п

### 68  Что определяет термин "чёрная высота" узла в структуре красно-черного дерева?

* Количество черных узлов на пути от корня до листового узла

### 69  чем смысл анализа алгоритма:

* Предсказать требуемые для его выполнения вычислительные ресурсы

### 70 При создании бинарного дерева поиска ключи поступали в следующей последовательности: 25, 15, 18, 10, 11, 7, 8, 17, 16. Из дерева удалили узел с ключом 18. Узел с каким значением был выбран замещающим?

* 17

### 71 Имеется идеально сбалансированное двоичное дерево, содержащее 31 узел. Сколько уровней в дереве?

* 5 уровней

### 72 При создании бинарного дерева поиска ключи поступали в следующей последовательности: 25, 12, 13, 10, 44, 11, 7, 8, 42. Какова высота этого дерева?

* 5

### 73 Дан рекурсивный алгоритм:
```
void F(int n){
    cout<<n<<endl;
    if (n<5){
        F(n+1);
        F(n+2);
        F(n+3);
    }
}
```
Найдите cyммy чисел, которые будут выведены при вызове F(2). (B поле ответа - только число)

* 79

### 74 Известно, что при построении хеш-таблиц возможно появление коллизий. Коллизия это такая ситуация, когда:

* ля разных ключей хэш-функция может принимать одно и тоже значение h(ki) = h(kj)

### 75 Какой из перечисленных алгоритмов реализует стратегию "жадных алгоритмов" (greedy algorithm)?

* алгоритм Дейкстры

### 76 Какой из видовВ линейных списков лучше использовать при реализации задачи по проверке баланса круглых скобок (соответствие открывающей и закрывающей скобок: (()()) — баланс)?

* стек (?)

### 77 Какой зависимостью описывается функция вычислительной сложности алгоритма турнирной сортировки в наихудшем случае

* f(n)=Ө(nlog(n))

### 78 К графу на рисунке 1 применен алгоритм обхода в глубину. Какое из остовных деревьев соответствует обходу B глубину с вершины 3?

* 5 (?)

### 79 Росту первичного кластера в хеш-таблице с открытым адресом способствует

* Для большого количества ключей хеш-функция сформировала один и тот же индекс (?)

### 80 Какой зависимостью описывается функция вычислительной сложности алгоритма поиска в глубину на списке смежных вершин, если V — количество вершин в графе, а Е -количество ребер:

* f(n)=O(V^2) (?)

### 81 Дана следующая рекурсивная функция
```
int fib(int n){
    if (n<2){return 1;}
    else {return fib(n-2)+fib(n-1);}
}
```

Какова высота дерева рекурсии при вызове fib(5)

* 4

### 82 Укажите свойство, характеризующее структуру данных Дек

* Обладает двумя первыми элементами

### 83 Для оценки порядка роста функций, описывающих вычислительную сложность алгоритмов, используются асимптотические обозначения (символики) или нотации. Что обозначает запись f(n)=Ө(g(n))

* Множество всех функций, порядок роста которых ограничен сверху и снизу функцией g(n) сточностью A0 постоянных множителей с1 и c2 соответственно

### 84 Что будет выведено при прямом обходе дерева?

* a b d e c f (?)

### 85 Для каких узлов необходимо проверить коэффициент балансировки (баланс-фактор) после добавления узла в АВЛ-дерево?

* Для всех предков нового узла вплоть до корня дерева

### 86 то будет результатом применения алгоритма RLE ("групповое кодирование") к сжатию текста AAAAAAAAAAAAAAAAAAABBBBBAAABBBBBBBC?

* 19A5B3A7B1C

### 87 Какие отношения между элементами неявно поддерживает структура данных массив

* Линейные (?)

### 88 Какой зависимостью описывается фуНКЦИЯ вычислительной сложности алгоритма быстрой сортировки методом Xoapa (quicksort) в среднем и наилучшем случаях?

* f(n)=Ө(nlog(n))

### 89 Какой код будет получен из входной строки символов ABCCCDDDDD при использовании алгоритма Хаффмана?

* 110 111 10 10 10 0 0 0 0 0 0

### 90 Имеется идеально сбалансированное двоичное дерево (He являющееся деревом поиска), содержащее целые числа. Симметричный просмотр дерева (слева-направо, in-oder обход) даёт следующий результат: 2, 4, 6, 8, 10, 12, 14. Какой узел является корнем дерева?

* 8

### 91 Какие основные операции следует учитывать при оценке временной (вычислительной) сложности алгоритмов сортировки:

* Операции сравнения и перемещения данных

### 92 Диапазон допустимых значений простого типа данных определяется разрядностью отдельного значения этого типа. В перечне примеров найдите неправильный вариант

* int — занимает 4 байта, следовательно, имеет диапазон от —2 148 483 647 до +2 147 483 647

### 93 Какая проблема может возникнуть в ›‹еш-таблице с открытым адресом, после удаления ключа из первичного кластера ключей‚ хешированных с одним индексом?

* Невозможность найти ключ в первичном кластере

### 94 При создании бинарного дерева поиска ключи поступали в следующей последовательности: 25, 12, 13, 10, 11, 7, 8, 44, 42. B это дерево был вставлен узел с ключом 6. В какое поддерево и какого узла был вставлен узел?

* В левое поддерево узла 7

### 95 Какой зависимостью описывается фуНКЦИЯ вычислительной сложности алгоритма интерполяционного поиска в среднем случае?

* f(n)=Ө(log(log(n)))

### 96 Чем может быть вызвано переполнение стека при выполнении рекурсивной функции с небольшой глубиной рекурсии

* Неправильно сформулированным условием завершения рекурсии (?)

### 97 меется линейный односвязный список из п (п>2) узлов. Структура узла списка:

```
struct Tnode{
    Tdata data;
    Tnode* next;
}
```

L - указатель на ero начало. Укажите группу операторов, которые обеспечат корректное удаление двух узлов из начала списка L:

* q1=L; g2=L->next; L=L->next->next; delete q1;delete q2;

### 98 Когда алгоритм считается корректным

* Когда для каждого ввода результатом его работы является корректный вывод 

### 99 С какой вычислительной сложностью выполняютсяЯ операции поиска, удаления и вставки B хеш-таблицах в лучшем случае?

* O(1)

### 100 Что проверяет условие Айверсона в алгоритме сортировки методом простого обмена

* Наличие обменов в текущем проходе по массиву

### 101 Показатели эффективности алгоритмов (или программ) включают

* Количество выполняемых операций и объем требуемой памяти

### 102 Чему равна сумма степеней вершин графа?

* двоенному числу его ребер

### 103 Какие из перечисленных АВЛ-деревьев не требуют проведения перестройки?

* 1, 2, 4

### 104 Какой алгоритм сортировки из перечисленных ниже будет самым эффективным на уже отсортированном массиве?

* вставками (?)

### 105 Какой вычислительной сложностью характеризуется алгоритм "грубой силы" (brute force attack) прямого поиска по образцу?

* O(n*m) (?)

### 106 Как называется определённая последовательность вычислительных шагов, преобразующих входные величины в выходные?

* алгоритм

### 107  Для выполнения какой операции B линейном двусвязном списке необходимо выполнить два “подготовительных" оператора
```
...
q->prev->next=q->next;
q->next->prev=q->prev
...
```
(а - указатель на некрайний узел списка)

* Удаление узла по указателю q

### 108 Время выполнения рекурсивной функции f(n) в наихудшем случае определяется рекуррентным соотношением

* O(n) (?)

### 109 Что делает следующая функция?

```
int trinity (int a, int b, int c){
    if (a>=b) return trinity (a, c, b);
    else return trinity (b, a, c);
}
```

* вычисляет среднее значение 

### 110 Система байтового кодирования символов А5С!! кодирует символы входной последовательности кодами

* фиксированной длины в один байт

### 111 Какое из перечисленных АВЛ-деревьев требует балансировки?

* 3

### 112 Префиксный код в теории кодирования - это

* кодовое слово переменной длины, с которого нe может начинаться другое кодовое слово того же кода 

### 113 Какая структура данных относятся к категории линейных списков

* массив
* дек

### 114 Какой алгоритм реализует стратегию "разделяй и властвуй" (divide апа conquer approach)?

* алгоритм быстрой сортировки Quick-sort

### 115 Остовное дерево - это

* Подграф, полученный путем удаления максимального числа ребер, без нарушения связности

### 116 Когда один алгоритм считается эффективнее другого:

* Если его вычислительная сложность в наихудшем случае имеет более низкий порядок роста

### 117 Сколько указателей используется в структуре узла стека? (Введите только число)

* 1

### 118 Выберите все характеристики хеш-функции, используемой в алгоритме поиска Рабина-Карпа

* полиномиальный хеш 
* модульная арифметика 
* кольцевой ("скользящий", "летящий")

### 119 Нелинейными называются структуры данных, в которых

* связи между элементами зависят от выполнения определенного условия

### 120 Что обеспечивает метод пробирования "двойное хеширование" при вставке элемента в хеш-таблицу, разрешающей коллизии по методу открытого адреса?

* Вычисление смещения, зависящее от значения ключа

### 121 Какой зависимостью описывается функция вычислительной сложности алгоритма интерполяционного поиска в худшем случае?

* f(n)=Ө(n)

### 122 B основе алгоритма Рабина-Карпа используется

* Хеш-функция

### 123 Каков максимальный размер кодового слова, полученного алгоритмом Шеннона-Фано, Aana
текста, частота появления символов B KOTOPOM определяется таблицей: а-11,и-8,пробел - 6, л-4, n-3, с-3, м-2, ы-2, н-1, ш-1, ч-1, k-1, у-1, т-1, ь-1?

* 6 (?)

### 124 Какой формулой определяется максимальное количество узлов в бинарном дереве высотой К (у корня высота 0)?

* 2^(k+1)-1

### 125 Суть алгоритма сортировки методом Шелла заключается в

* Отдельной сортировке элементов, отстоящих друг от друга на расстоянии h, уменьшающемся на каждом проходе массива до значения 1

### 126 В АВЛ-дерево, содержащее ключи 8,10,11,14, был вставлен ключ 12. Потребовалась ли перестройка дерева и если дa, то каким способом?

* да, двойной LR (?)

### 127 МЭТОД сортировки считается устойчивым, если

* относительное расположение элементов с равными ключами не изменяется

### 128  результате применения алгоритма RLE был получен сжатый текст 9A-4BCAB7C. Какой текст был сжат этим алгоритмом?

* AAAAAAAAABCABCCCCCCC

### 129 Для структуры данных стек справедливо

* Линейный список с одной вершиной

### 130 Какая структура данных используется для хранения элементов данных в хеш—табпице‚ при реализации разрешения коллизий по методу цепочек (цепного хеширования)?

* Однонаправленный список

### 131 Для структуры данных дек справедливо

* Вставка нового элемента возможна в начало или в конец списка

### 132 Бинарное дерево - это

* дерево, у которого каждый узел может содержать до двух дочерних узлов

### 133 Какие нотации можно использовать для асимптотической оценки сложности алгоритма в лучшем случае?

* Ω (?)
* O
* Ө

### 134 Зачем нужен заглавный элемент в двусвязном кольцевом списке?

* Для связи последнего и первого элемента 

### 135 Когда достигается максимальная эффективность алгоритма Бойера-Мура?

* Если образец длинный, а мощность алфавита достаточно велика

### 136 Какой зависимостью описывается фуНКЦИЯ вычислительной сложности алгоритма сортировки методом простого обмена (с условием Айверсона) в наилучшем случае

* f(n)=O(n)

### 137 K недостаткам рекурсивного метода можно отнести

* возможность переполнения стековой памяти программного процесса
* расход времени на выделение и очистку стекового кадра в памяти (?)
* возможность переполнения динамически распределяемой памяти программного процесса

### 138 Какую операцию можно использовать в качестве хеш-функции для целочисленных ключей?

* Операцию взятия остатка от деления ключа на 10 правильность размерность массива 

### 139 Какая вычислительная сложность описывает реализацию алгоритма Дейкстры для графов, представленных в виде списков смежности (n и т обозначают соответственно число вершин и ребер исходного графа):

* O(n^2)

### 140 Имеется указатель д на узел в середине линейного односвязного списка со следующей структурой узла

```
struct Tnode{
    Tdaa data;
    Tnode* next;
}
```

Требуется вставить новый узел (узел содержит AaHHbIE), ссылку на который хранит указатель qq, в позицию, B которой находится узел д. Какую последовательность операторов необходимо выполнить, чтобы корректно выполнилась данная операция вставки?

* qq->next=q->next; q->next=qq; swap(qq->data, q->data)

### 141 Какие поля должны содержать записи хешйтаблицы при использовании метода цепочек (цепного хеширования)?

* Указатель на начало списка элементов с одним и тем же значением хеш-функции

### 142 Временная (вычислительная) сложность алгоритма определяется объёмом входа n. Этот параметр в частном случае может определяться

* Размером обрабатываемого массива или файла

### 143 Как называют множество элементов данных и внутренних связей между ними

* структура данных

### 144 Имеется бинарное дерево поиска, содержащее целые числа от 1 до 7. Каким будет результат вывода при обратном обходе дерева (postoder):

* 7, 6, 5, 4, 3, 2, 1

### 145 Какой зависимостью описывается фуНКЦИЯ вычислительной сложности алгоритма Дейкстры в графе, построенном на списке смежных вершин, если \ - количество вершин в графе‚ a E- количество ребер

* f(n)=O(v^2)

### 146 Алгоритм обхода графа - это

* Алгоритм, устанавливающий движение от начальной вершины B определенном направлении (по определенному пути) до тех пор, пока не будет достигнут конец пути или заданная вершина

### 147 Рекурсия имеет место, если решение задачи сводится к разделению её на меньшие подзадачи, выполняемые с помощью одного и того же алгоритма. Когда должен завершиться правильно организованный процесс разбиения задачи на подзадачи

* Когда достигается простейшее возможное решение

### 148 Высота (глубина) бинарного дерева поиска из N узлов в наилучшем случае определяется по формуле:

* h=log2(N+1)

### 149 алгоритмы сортировки выбором, обменом и вставкой называются простыми в смысле их

* интеллектуальной сложности (?)

### 150 Укажите асимптотическую сложность операции удаления элемента в позиции i из массива размером n:

* O(n)

### 151 Какой из приведенных кодов является префиксным (коды отдельных кодовых слов перечислены через запятую)?

* 0, 100, 101, 110, 111

### 152 Как определить, что функция реализует каскадную (множественную) рекурсию

* Функция вызывает несколько внешних функций по любой из всех возможных ветвей алгоритма 

### 153 Определите теоретическую вычислительную сложность (функцию роста времени) алгоритма

```
int count=1;
while (count<n){
    count=count*2;
    / * Последовательность шагов программы с временной сложностью O(1)* /
}
```

* логарифмическая

### 154 Какой код будет получен из входной строки символов 1111223444 при использовании алгоритма группового кодирования RLE (Run Length Encoding)?

* 41, 22, 13, 34

### 155 Какой зависимостью описывается функция вычислительной сложности алгоритма быстрой сортировки методом Шелла в наихудшем случае

* f(n)=O(n^2)

### 156 Алгоритм Флойда-Уоршала - это

* Алгоритм для нахождения кратчайших пу'гей между всеми вершинами взвешенного ориентированного графа

### 157 Структура хранения, в которой элементы данных физически размещаются в последовательных ячейках памяти строго один за другим:

* векторная

### 158 Имеется описание структуры узла линейного односвязного списка

```
struct Tnode{
    Tdata data;
    XXXX next;
}
```
Какое определение должно быть у экземпляра этой структуры на месте XXXX?
* *Tnode

### 159 Имеется бинарное дерево поиска, содержащее целые числа. Просмотр дерева даёт следующий результат: 2, 4, 6, 8, 10, 12, 14. Какой способ обхода дерева при этом использовался

* симметричный

### 160 Какой зависимостью описывается функция вычислительной сложности алгоритма линейного (последовательного) поиска в худшем случае?

* f(n)=Ө(n)

### 161 Имеется узел АВЛ-дерева, для которого |hl-hr|=0, где hl, hr высота левого и правого поддерева соответственно. В левое поддерево вставляется новый ключ, после этого

* требуется перестройка дерева 

### 162 Что означает утверждение, что алгоритм X асимптотически более эффективен, чем алгоритм У?

* X 6удет лучшим выбором ana любых входных данных, начиная с некоторого граничного значения

### 163 Какое количество узлов в полном бинарном дереве высотой 4?

* 31

### 164 Какой зависимостью описывается фуНКЦИЯ вычислительной сложности алгоритма прямого поиска в тексте по образцу в худшем случае?

* f(n, m)=Ө(log(n*m))

### 165 Укажите асимптотическую сложность операции удаления элемента, на КОТОРЫЙ имеется указатель (например, ptr), U3 двунаправленного списка размером n

* O(1)

### 166 Какое из условий проверяется при определении сбалансированности красно-черного дерева?

* Любой путь от корня дерева K листу содержит одно и TO же число черных узлов

### 167 Имеется некоторая структура данных, в которую заносятся упорядоченные по возрастанию символы. Считывание данных из этой структуры даёт результат: Е, Е, D, С, B, А. Чем является эта структура данных?

* стек

### 168 Имеется двоичное дерево поиска, содержащее целые числа. Обратный обход дерева даёт следующий результат: 10, 30, 20, 50, 70, 60, 40. Какой узел является корнем дерева?

* 40

### 169 Какие из приведённых строк кода
```
int fib(int n){
    if (n<2)
        return 1;
    else {...}
}
```
необходимо записать после else в рекурсивной функции вычисления п-ного числа Фибоначчи?

* return fib (n-1)+fib(n-2)

### 170 Прямой доступ к элементу данных B памяти делает возможным

* адрес элемента в ОЗУ

### 171 Какой зависимостью описывается фуНКЦИЯ вычислительной сложности алгоритма сортировки методом простого выбора в наилучшем случае

* f(n)=O(n^2)

###172 Что необходимо делать с большим объемом информации, связанной с каждым ключом

* Не хранить ее в хеш-таблице (?)

### 173 Дан рекурсивный алгоритм
```
inf F(int n)
{
    if (n>2)
        return F(n-1)+F(n-2)+F(n-3);
    else return n;
}
```

Найдите значение F(5). (B поле ответа - только число)

* 11

### 174 Какой зависимостью описывается фуНКЦИЯ вычислительной сложности алгоритма Дейкстры B графе, построенном на матрице смежности, если V - количество вершин B rpade, а Е — количество ребер

* f(n)=O(V^2)

### 175 Специфичным для обхода графа, B отличие от обхода дерева, является

* в графе могут быть циклы

### 176 Структура хранения данных, в которой элементы явно хранят связи с соседними элементами структуры

* списочная

### 177 Каков главный недостаток хеш-таблиц

* Фиксированный размер, при неизвестном окончательном числе элементов 

### 178 Свойство алгоритма, обеспечивающее возможность его использования для решения множества однотипных задач (применимость к различным наборам входных данных), называется:

* массовость

### 179 Какой из методов поиска обычно используется при поиске кратчайшего пути между любыми двумя вершин во взвешенном неориентированном графе?

* Поиск методом в ширину

### 180 Перед выполнением какой операции в линейном двусвязном списке необходимо выполнить два "подготовительных" оператора:

```
...
q->prev->next=q->next;
q->next->prev=q->prev,
...
```

(q - указатель на некрайний узел списка)

* Удаление узла по указателю q

### 181 Что такое инвариант цикла

* Логическое выражение, истинное перед началом цикла, после каждого прохода тела цикла и B конце выполнения цикла, зависящее от переменных, изменяющихся в теле цикла

### 182 Имеется некоторая линейная структура данных, в которую заносятся упорядоченные по возрастанию символы. Считывание данных U3 этой структуры даёт результат: Е, Е, D, C, B, А. Чем может быть эта структура данных?

* дек
* стек

### 183 Количество ребер в остовном дереве (п - количество вершин графа) равно

* n-1

### 184 Какая идея положена в основу построения алгоритма методом "жадного алгоритма"

* На каждом этапе осуществляется выбор решения, которое кажется оптимальным в данный момент

### 185 Чтобы алгоритм Дейкстры сформировал помимо длины кратчайшего пути от вершины а к вершине b и сам путь в графе, предусматривают запись вершин в линейную структуру. B какой последовательности расположатся вершины пути в структуре?

* Последовательно от вершины b до а

### 186 Маршрутом в графе называется:

* последовательность вершин графа‚ B КОТОрОЙ каждая вершина соединена с предыдущей ребром

### 187 Что хранит матрица смежности графа

* Список ребер и их направление

### 188 Как называется алгоритм для нахождения кратчайших путей между всеми вершинами взвешенного ориентированного графа?

* Алгоритм Флойда-Уоршала

### 189 Какой зависимостью описывается функция вычислительной сложности алгоритма быстрой сортировки методом Хоара (quicksort) в наихудшем случае:

* f(n)=O(n^2) (?)

### 190 Как определить есть ли B неориентированном CBA3HOM графе Эйлеров цикл?

* B графе все вершины имеют четную степень

### 191 Какая вычислительная сложность характерна для алгоритмов со стратегией "разделяй и властвуй" (divide апа conquer approach)?

* O(nlogn) (?)

### 192 Линейными называются структуры данных, в которых:

* связи между элементами не зависят от выполнения какого-либо условия

### 193 Какой зависимостью описывается функция вычислительной сложности алгоритма сортировки методом простой вставки в наилучшем случае:

* f(n)=O(n)

### 194 Какой зависимостью описывается фуНКЦИЯ вычислительной сложности алгоритма поиска в ширину в неориентированном графе, построенном на списке смежных вершин, если V — количество вершин в графе, а Е -количество ребер:

* f(n)=(V+E)

### 195 Для динамической структуры данных (переменной) справедливо утверждение:

* создается на этапе выполнения программы и размер может быть изменен на этапе выполнения (?)

### 196 Как называется алгоритм для нахождения кратчайших путей от одной из вершин графа до всех остальных?

* Алгоритм Дейкстры

### 197 Какое из определений соответствует структуре данных граф?

* Нелинейная, многосвязная, динамическая структура данных

### 198 Какой зависимостью описывается функция вычислительной сложности алгоритма Флойда- Уоршала в графе, построенном на матрице смежности, если V — количество вершин в графе, а Е —количество ребер:

* f(n)=O(V^3)

### 199 Структура хранения, обеспечивающая прямой доступ к каждому элементу данных, т.е. время доступа к значению О(1):

* векторная

### 200 Какая пространственная сложность описывает матрицу смежности графа как функцию от числа n вершин и числа m ребер:

* O(n^2) (?)